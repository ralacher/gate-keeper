services:
  gate:
    build: .
    container_name: gate-keeper
    restart: unless-stopped
    ports:
      - "3080:80"
    environment:
      # These are used by nginx reverse proxy at runtime (envsubst)
      - HA_BASE_URL=http://192.168.0.201:8123
      - GO2RTC_URL=http://192.168.0.201:1984
